{
  "name": "my-little-community",
  "version": "0.1.0",
  "private": true,
  "description": "A cozy sandbox where prompt-defined agents wander a meadow and talk to each other.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c green,magenta \"npm:dev -w server\" \"npm:dev -w client\"",
    "build": "npm run build -w server && npm run build -w client",
    "test": "npm run test -w client && npm run test -w server",
    "typecheck": "npm run typecheck -w client && npm run typecheck -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
