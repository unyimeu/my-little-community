# Copy this file to `.env` (in the server/ folder) and fill in your key.
# This is the ONLY place the OpenAI key lives — it never reaches the browser.

OPENAI_API_KEY=sk-your-key-here

# Optional overrides:
OPENAI_MODEL=gpt-4o-mini
PORT=8787
